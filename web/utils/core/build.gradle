project(':web:utils:core') {
    dependencies {
        api("org.slf4j:slf4j-api:$slf4j_version") {
            exclude group: 'org.slf4j', module: 'logback'
        }

        api("org.springframework:spring-context:$spring_version")
    }
}
